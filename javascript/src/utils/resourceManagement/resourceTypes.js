// src/resourceTypes.js

/**
 * @typedef {Object} Resources
 * @property {number} foodUsed - The amount of food used.
 * // ... other properties of resources
 */

// Define default Resources object
const defaultResources = {
  foodUsed: 0,
  // Initialize other properties with default values
};

module.exports = { defaultResources };
