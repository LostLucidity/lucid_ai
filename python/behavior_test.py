# from sc2.constants import AbilityId

# from behavior import cancel_construction

# def test(self):
#   test_results = []
#   failed_tests = []
#   # should cancel construction when in range of enemy attacking it.
#   test_results += cancel_when_enemy_in_range(self)

# def cancel_when_enemy_in_range(self):
#   test_results = []
#   unit = # get baneling example, radius
#   enemy_unit = # get marine example, radius, range
#   self.all_enemy_units_and_structure = [ enemy_unit ]
#   print('unit(AbilityId.CANCEL)', unit(AbilityId.CANCEL))
#   cancel_construction(self, unit)[0] === unit(AbilityId.CANCEL)
#   test_results.append = { 'cancel_when_enemy_in_range': cancel_construction(self, unit) }
